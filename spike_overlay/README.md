# Spike-only cmpx575 launcher overlay bundle

This directory mirrors `cmpx575/centaur-overlay@84de279` so `spike-centaur` can mount the tested launcher workflow and typed tool from a cmpx-owned ref reachable by the cluster repo-cache. It is an ordered final source only; it does not replace or retire the canonical overlay.
